var Tree = React.createClass({
  getInitialState: function() {
    return {
    };
  },

  render: function() {
    return <pre>hi</pre>;
  }
});

React.render(
  <Tree />,
  document.getElementById('rightpane')
);
